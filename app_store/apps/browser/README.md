# Browser

Remote Chromium provided by the LinuxServer Chromium image. It persists browser
state in app data and maps the shared workstation volume to Downloads.

The app needs WebSockets and a relatively large shared-memory allocation.
Verify navigation, downloads, and VPN exit IP before publishing an update.
